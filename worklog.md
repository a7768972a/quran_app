---
Task ID: 1
Agent: Main (orchestrator)
Task: بناء نظام تسجيل حضور الطلاب بالكامل (4 أقسام) — واجهة عربية RTL

Work Log:
- فحص بنية المشروع Next.js 16 وتأكيد توفّر Prisma + SQLite و shadcn/ui.
- كتابة مخطط Prisma: نموذج Student (name, age, lessonDay) ونموذج Record (memorization, grade, homework, date) مع علاقة cascade وفهارس.
- تنفيذ `bun run db:push` بنجاح.
- إنشاء API routes:
  - `src/app/api/students/route.ts` (GET/POST)
  - `src/app/api/students/[id]/route.ts` (GET/PUT/DELETE) مع جلب السجلات
  - `src/app/api/records/route.ts` (GET بتصفية يومية/أسبوعية + POST)
  - `src/app/api/stats/route.ts` (إحصائيات لوحة التحكم + توزيع الدرجات + سجلات أسبوعية + أحدث السجلات)
- إضافة `src/lib/constants.ts` (أيام الدرس، الدرجات مع ألوان) و `src/lib/date.ts` (دوال تواريخ عربية + حساب بداية الأسبوع = السبت).
- تحديث `layout.tsx`: `dir="rtl" lang="ar"` + خط Tajawal العربي + Sonner Toaster.
- تحديث `globals.css`: نظام ألوان أخضر (emerald/teal) يناسب سياق التحفيظ + شريط تمرير مخصص.
- بناء `src/app/page.tsx`: تخطيط رأس ثابت + تبويبات أربعة + تذييل ثابت (mt-auto).
- بناء أقسام الواجهة:
  - `dashboard-section.tsx`: 4 بطاقات إحصائية + أزرار وصول سريع + رسم بياني للأسابيع + فطيرة للدرجات + توزيع الطلاب حسب اليوم + أحدث السجلات.
  - `students-section.tsx`: قائمة بطاقات + بحث + نموذج إضافة/تعديل (Dialog) + تأكيد حذف (AlertDialog) + بروفايل مع سجل الحفظ.
  - `recording-section.tsx`: بحث واختيار طالب + نموذج (الحفظ/الدرجة radio/الوظيفة/التاريخ) + رسالة نجاح.
  - `info-section.tsx`: تنقل أسبوعي (سابق/تالي) + اختيار تاريخ + إحصائيات الأسبوع + تجميع السجلات حسب اليوم (السبت/الثلاثاء + أي يوم به سجلات).
- إصلاح: قسم المعلومات كان يعرض السبت والثلاثاء فقط فأخفى السجلات المسجلة في أيام أخرى → عدّلت الفلتر ليعرض أيام الدرس + أي يوم به سجلات.
- التحقق بـ agent-browser: فتح الصفحة، اختبار الإضافة (طالب "محمد أحمد")، تسجيل حفظ (ممتاز)، ظهوره في قسم المعلومات وبروفايل الطالب، ولوحة التحكم تعرض الإحصائيات الصحيحة.
- التحقق البصري بـ VLM: تصميم نظيف، RTL صحيح، متجاوب على الجوال (390px)، التذييل ثابت.
- `bun run lint` نظيف بدون أخطاء.

Stage Summary:
- النظام يعمل بالكامل end-to-end ومتحقَّق منه في المتصفح.
- قاعدة البيانات: Prisma + SQLite (محلياً). ملاحظة للمستخدم: المشروع مهيّأ بـ Prisma+SQLite وليس Supabase؛ يمكن الترحيل لاحقاً.
- جميع المتطلبات الأربعة (لوحة التحكم، الطلاب، تسجيل الحفظ، المعلومات الأسبوعية) مكتملة.
- لم تُكتشف أخطاء runtime أو مشاكل hydration.
- نتيجة اختبار: طالب + سجل حفظ تم إنشاؤهما وظهرا في كل الأقسام بشكل صحيح.
